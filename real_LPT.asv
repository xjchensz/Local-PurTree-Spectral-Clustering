%test LPT
%input 5 csv files
%
%%
clc;
%clear all;

folder_now = pwd;
addpath([folder_now,'\real_data']);
addpath([folder_now, '\functions']);


level=5;

for i=1:level
    num=csvread([ 'l' num2str(i) '.csv']);
    D(:,:,i)=num;
end

sprintf('The number of objects is: %d', size(D,1))

[y, P, W, Z, evs]=LPT(D,10,50,500);

%P
figure('name','Learned graph by CAN'); 
imshow(P,[]); colormap jet; colorbar;


%%